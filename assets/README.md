# Assets

Place the main Galexia glTF/GLB model here:

```txt
assets/galexia.glb
```

The current web app will attempt to load this file automatically. If it is missing, the app renders a procedural galaxy fallback scene.
